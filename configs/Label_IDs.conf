## mission Node Id's
rescue_soldier, scan_zone, identify_agent

## action Node ID's
send_helicopter, create_rescue_path, send_drone, monitor_drone, check_drone_report, classify_soldier

## observation Node ID's
soldier_found, zone_scanning, zone_reporting, zone_scanned, agent_moving, agent_shot, agent_idle, agent_state

## ui_component Node ID's
default_minimap_component, default_zone_component, default_button_component, default_drag_drones_component,
default_stage_manager_component, default_dashboard_component, default_task_stepper_component,
default_drone_status_component, default_urgent_action_component, default_logbook_component


